name: Character Decision Commitment Reservoir

on:
  pull_request:
    paths:
      - 'docs/character-decision-commitment-reservoir-v1.md'
      - 'src/game/data/characterDecisionCommitmentReservoir.ts'
      - 'scripts/quality/check-character-decision-commitment-reservoir.ts'
      - '.github/workflows/character-decision-commitment-reservoir.yml'
  push:
    branches: [main]
    paths:
      - 'docs/character-decision-commitment-reservoir-v1.md'
      - 'src/game/data/characterDecisionCommitmentReservoir.ts'
      - 'scripts/quality/check-character-decision-commitment-reservoir.ts'
      - '.github/workflows/character-decision-commitment-reservoir.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate 36-character decision/commitment reservoir
        run: node --experimental-strip-types scripts/quality/check-character-decision-commitment-reservoir.ts
