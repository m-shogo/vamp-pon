name: Runtime Projectile Recovery Payload

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  payload-reference-only:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: |
          python - <<'PY'
          from pathlib import Path
          exec(compile(Path('.github/pr169-runtime-recovery.py').read_text(), '.github/pr169-runtime-recovery.py', 'exec'))
          PY
