name: Character Address Naming Register Reservoir

on:
  pull_request:
    paths:
      - 'docs/character-address-naming-register-reservoir-v1.md'
      - 'src/game/data/characterAddressNamingRegisterReservoir.ts'
      - 'scripts/quality/check-character-address-naming-register-reservoir.ts'
      - '.github/workflows/character-address-naming-register-reservoir.yml'
  push:
    branches: [main]
    paths:
      - 'docs/character-address-naming-register-reservoir-v1.md'
      - 'src/game/data/characterAddressNamingRegisterReservoir.ts'
      - 'scripts/quality/check-character-address-naming-register-reservoir.ts'
      - '.github/workflows/character-address-naming-register-reservoir.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Validate 36-character address/naming/register reservoir
        run: node --experimental-strip-types scripts/quality/check-character-address-naming-register-reservoir.ts
